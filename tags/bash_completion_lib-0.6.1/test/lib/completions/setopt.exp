proc setup {} {
}; # setup()


proc teardown {} {
}; # teardown()


setup


set test "Tab should complete"
    # Try completion
set cmd "setopt nounse"
send "$cmd\t"
expect {
    -re "^setopt nounset +$" { pass "$test" }
    -re /@ { fail "$test at prompt" }
    -re eof { fail "eof" }
}; # expect


# Interrupt command by sending ^C

sleep 0.1; 
send \003;  # Control-c
expect -ex /@


teardown
