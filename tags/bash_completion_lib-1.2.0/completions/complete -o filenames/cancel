# CUPS cancel(1) completion
#
_cancel()
{
    local cur

    COMPREPLY=()
    cur=`_get_cword`

    COMPREPLY=( $( lpstat | cut -d' ' -f1 | grep "^$cur" ) )
} # _cancel()

comp_install  # Make completion persistent for subsequent invocations

_cancel  # Generate completions for first invocation
